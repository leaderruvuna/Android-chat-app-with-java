<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/chat_prof_image"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginTop="18dp"
        android:src="@drawable/avatar" />

    <TextView
        android:id="@+id/userMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/chat_prof_image"
        android:layout_toEndOf="@+id/chat_prof_image"
        android:layout_toRightOf="@+id/chat_prof_image"
        android:background="@drawable/messagelayoutstyle"
        android:gravity="center"
        android:maxWidth="190dp"
        android:minWidth="100dp"
        android:paddingBottom="15dp"
        android:paddingTop="15dp"
        android:textColor="#fff" />



    <ImageView
        android:id="@+id/imageMessage"
        android:layout_width="160dp"
        android:layout_height="60dp"
        android:paddingTop="5dp"
        android:paddingBottom="5dp"
        android:layout_alignTop="@+id/chat_prof_image"
        android:layout_toEndOf="@+id/chat_prof_image"
        android:layout_toRightOf="@+id/chat_prof_image"
        android:background="@drawable/imagemessagestroke"

         />

    <TextView
        android:id="@+id/messageTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/userMessage"
        android:layout_alignEnd="@+id/userMessage"
        android:layout_alignRight="@+id/userMessage"
        android:layout_marginRight="-10dp"
        android:textColor="#dcdcdc"
        android:textSize="10dp"
        android:text="date" />


</RelativeLayout>